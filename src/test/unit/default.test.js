// const assert = require('assert')

// describe('Array', _ => {
    // describe('#indexOf()', _ => {
        // it('should return -1 when the values is not present', _ => {
            // assert.equal([1,2,3].indexOf(4), -1)
            // _()
        // })
    // })
// })
